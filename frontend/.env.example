# Optional: API key for geolocation reverse geocoding
# Only needed if you want the "Use my location" feature to work
VITE_WEATHER_API_KEY=your_api_key_here

# Backend API URL (for production deployment)
# Leave empty for local development (uses /api proxy)
VITE_API_URL=
